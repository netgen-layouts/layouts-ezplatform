{{ render(
    controller(
        'ez_content:viewAction', {
            'contentId': item.object.contentInfo.id,
            'location': item.object,
            'viewType': viewType,
            'layout': false
        }
    )
) }}
