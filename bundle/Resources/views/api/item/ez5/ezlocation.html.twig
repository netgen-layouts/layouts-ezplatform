{# @deprecated #}

{{ render(
    controller(
        'ez_content:viewLocation', {
            'contentId': item.object.contentInfo.id,
            'location': item.object.id,
            'viewType': 'ngbm_app_preview',
            'layout': false,
        }
    )
) }}
