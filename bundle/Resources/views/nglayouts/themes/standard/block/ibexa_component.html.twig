{% extends '@nglayouts/block/block.html.twig' %}

{% block content %}
    {% if not block.parameter('content').empty %}
        {{ render(
            controller(
                'ibexa_content:viewAction', {
                    'contentId': block.parameter('content').value,
                    'viewType': block.viewType,
                    'layout': false,
                    'params': {
                        'block': block,
                    }|merge(ibexa_params|default({}))
                }
            )
        ) }}
    {% endif %}
{% endblock %}
