{# @deprecated #}

{{ render(
    controller(
        'ez_content:viewLocation', {
            'contentId': item.object.contentInfo.id,
            'locationId': item.object.id,
            'viewType': viewType,
            'layout': false,
        }
    )
) }}
