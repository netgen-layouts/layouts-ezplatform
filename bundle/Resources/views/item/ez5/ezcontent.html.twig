{# @deprecated #}

{{ render(
    controller(
        'ez_content:viewLocation', {
            'contentId': item.object.id,
            'locationId': item.object.mainLocationId,
            'viewType': view_type,
            'layout': false,
            'params': {
                'ngbm_item': item,
            }
        }
    )
) }}
